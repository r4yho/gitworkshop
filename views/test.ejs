<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>trialejs</title>
    <style>
        body {
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364, #9cddf9);
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', sans-serif;
        }

        .rainbow-text {
            font-size: 5rem;
            font-weight: 900;
            /* 1. Create the rainbow background */
            background: linear-gradient(to right, #ef5350, #f48fb1, #7e57c2, #2196f3, #26c6da, #43a047, #eeff41);
            
            /* 2. Clip the background to the text */
            -webkit-background-clip: text;
            background-clip: text;

            /* 3. Make the actual text transparent so the background shows through */
            color: transparent;
            
            /* Extra: A subtle glow to make it pop against the dark background */
            filter: drop-shadow(0 2px 10px rgba(0,0,0,0.3));

            margin-top: -100px;
        }

              .photo {
        margin-top: 300px;
      }

      .photo img {
        max-height: 40vh; 
        width: auto;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        transition: transform 0.3s ease;
      }

      .photo img:hover {
        transform: scale(1.05);
      }

      @media (max-width: 600px) {
        .container {
          padding: 15px;
          margin: 10px;
        }
        .photo img {
          max-height: 30vh;
          max-width: 100%;
        }
      }
    </style>
</head>
<body>

    <div class="rainbow-text">
        ARE YOU DONE?
    </div>

    <div class="photo">
        <img src="/sleepy.gif" alt="Sloth GIF" />
    </div>
</body>
</html>